# Shift at Midnight Internal Links

## Hub Links

Homepage links to:

- `/demo/`
- `/beginner-guide/`
- `/customer-checks/`
- `/co-op/`
- `/survival-phase/`
- `/platforms/`
- `/release-date/`
- `/sources/`

## Contextual Links

`/release-date/`:

- link to `/platforms/` for Xbox/Steam/Game Pass status;
- link to `/demo/` when users want something playable now;
- link to `/sources/` for conflict details.

`/demo/`:

- link to `/beginner-guide/` for store task and shift workflow;
- link to `/customer-checks/` for inspection basics;
- link to `/co-op/` for multiplayer preparation;
- link to `/survival-phase/` for danger response basics;
- link to `/sources/` for Steam demo source.

`/platforms/`:

- link to `/release-date/` for date/status conflict;
- link to `/demo/` for Steam demo;
- link to `/sources/` for platform source matrix.

`/beginner-guide/`:

- link to `/customer-checks/` for the inspection workflow;
- link to `/survival-phase/` for danger response;
- link to `/co-op/` for multiplayer roles;
- link to `/joes-diner-newsletter/` for the rising query FAQ;
- link to `/sources/` for what is official vs held.

`/customer-checks/`:

- link to `/beginner-guide/` for the broader shift context;
- link to `/survival-phase/` for what happens when danger is triggered;
- link to `/co-op/` for team communication during inspection;
- link to `/joes-diner-newsletter/` for query-specific FAQ;
- link to `/sources/` for video/source boundaries.

`/co-op/`:

- link to `/demo/` for demo availability;
- link to `/customer-checks/` for inspection categories;
- link to `/beginner-guide/` for shift task context;
- link to `/sources/` for platform multiplayer confirmation.

`/survival-phase/`:

- link to `/beginner-guide/` for the shift context;
- link to `/customer-checks/` for what triggers danger;
- link to `/co-op/` for team communication during survival;
- link to `/sources/` for official danger-phase description.

`/joes-diner-newsletter/`:

- link to `/customer-checks/` for game-loop context;
- link to `/release-date/` because related queries include release timing;
- link to `/sources/` to explain why the exact answer is held if unverified.

`sources/`:

- link back to all content pages with "where this source is used" rows.

## Avoid

- Do not create isolated thin pages for `PS5`, `Game Pass`, `Joe's Diner`, or `release date` variants if the content can live inside existing stronger pages.
- Do not link to HOLD-only walkthrough or spoiler pages in v1.
- Do not add co-op strategy specifics beyond role categories and proximity chat implications.
- Do not add exact survival mechanics beyond the official "board up / set traps / hide" list.
